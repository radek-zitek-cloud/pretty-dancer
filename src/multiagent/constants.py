"""Application-wide constants for the multi-agent system.

Only true constants belong here — values that are never configurable and never
change at runtime. All other values belong in settings.py.
"""

from __future__ import annotations

APP_NAME: str = "multiagent"
