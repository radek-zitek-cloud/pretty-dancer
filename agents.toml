[agents.editor]
router = "editorial_gate"       # dynamic routing — keyword match on output

[agents.writer]
next_agent = "linguist"

[agents.linguist]
next_agent = "human"            # finished article goes back to human

[routers.editorial_gate]
type = "keyword"
routes.writer = ["WRITER BRIEF", "END BRIEF"]
default = "human"
